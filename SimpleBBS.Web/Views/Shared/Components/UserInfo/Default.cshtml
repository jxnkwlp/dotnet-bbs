@model UserBaseViewModel


<div class="panel panel-default">
    <div class="panel-heading">用户信息</div>
    <div class="panel-body user-info">
        <div href="#" class="avatar">
            <img src="@(Model.Email.GeneralAvatarUrl())" /><a asp-route="user" asp-route-id="@(Model.UserName)">@(Model.UserName)</a>
        </div>
        <p>“@Model.UserSign”</p>
    </div>
</div>
